# console_js_runner

A command-line JavaScript runner powered by `dart_qjs`.

Run it from this directory:

```bash
dart run
```

Enter JavaScript code at the `js>` prompt. Finish the current script with an
empty line, or type `:quit` to exit.
