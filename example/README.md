# Examples

This directory contains two `dart_qjs` example applications.

- `console_js_runner`: a Dart command-line JavaScript runner.
- `flutter_js_runner`: a Flutter JavaScript runner with an editor and output panel.

Both examples use a path dependency that points back to the package root.
