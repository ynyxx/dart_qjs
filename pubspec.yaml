name: dart_qjs
description: A Dart package for running JavaScript with QuickJS.
version: 1.0.0
repository: https://github.com/ynyxx/dart_qjs

environment:
  sdk: ^3.11.4

# Add regular dependencies here.
dependencies:
  native_toolchain_cmake: 0.2.5
  hooks: ^1.0.3
  logging: ^1.3.0
  ffi: ^2.2.0
  code_assets: ^1.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
